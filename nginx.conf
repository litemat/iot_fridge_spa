server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        
        # ЭТО ГЛАВНАЯ ЧАСТЬ: перенаправляет все запросы на index.html
        try_files $uri $uri/ /index.html;
    }
}